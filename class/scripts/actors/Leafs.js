(function(){
	var global = (1,eval)('this');

	/**
	 * predefine
	 */
	if (global.scripts.Portal) { return; }

	global.scripts.Leafs = {
		initialize : function (host, manager) {
			//смотрим на настройки и ставим нужную направление
		}
	}
}());